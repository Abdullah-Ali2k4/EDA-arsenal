# Essential functions to build:
1. eda_report(df) - One-function summary
2. plot_correlation_matrix(df, method='spearman')
3. missingness_heatmap(df)
4. plot_distribution_grid(df, cols=5)
5. detect_leakage(X, y, threshold=0.8)
6. statistical_summary(df)
7. memory_reducer(df)
8. outlier_detector(df, method='iqr')